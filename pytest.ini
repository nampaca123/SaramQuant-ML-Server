[pytest]
testpaths = tests
# data_source_test는 외부 API를 직접 호출하는 수동 스크립트라 수집 대상에서 제외한다
addopts = -m "not integration" --ignore=tests/data_source_test
markers =
    integration: touches real AWS resources (run explicitly with -m integration)
